type Post {
  id: ID!
  title: String!
  content: String!
  published: Boolean!
  createdAt: String!
  userId: Int!
}

input NewPost {
  title: String!
  content: String!
  userId: Int!
}

# type Mutation {
#   createPost(input: NewPost!): Post!
# }

type Query {
  test: String
}

type Subscription {
  postAdded: Post!
}
