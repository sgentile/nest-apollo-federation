type Post {
  id: ID!
  title: String!
  content: String!
  published: Boolean!
  createdAt: String!
}

input NewPost {
  title: String!
  content: String!
}

# type Mutation {
#   createPost(input: NewPost!): Post!
# }

type Query {
  test: String
}

type Subscription {
  postAdded: Post!
}
